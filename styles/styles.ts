import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    col: {
        display: 'flex',
        flexDirection: 'column',
      },
    row: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
    }
})